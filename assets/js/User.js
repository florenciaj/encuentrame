export class User {
    constructor(idUser, name, number, email, password) {
        this.idUser = idUser;
        this.name = name;
        this.number = number;
        this.email = email;
        this.password = password;
    }
}